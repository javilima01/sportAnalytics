[pytest]
testpaths = tests
markers =
    integration: real CPU inference and one-epoch training (no pretrained downloads)
    gui: optional smoke test that opens a real desktop window
    network: optional live YouTube download and pretrained tagging test
